apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubetask.fullname" . }}
  labels:
    {{- include "kubetask.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "kubetask.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: controller
